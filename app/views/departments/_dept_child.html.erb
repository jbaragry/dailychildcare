
  <%= image_tag("http://www.gravatar.com/avatar/" + Digest::MD5.hexdigest(child.name) + "?d=monsterid",
    :alt => "name",
    :class => "image") %>
  <br/>
  <%= link_to h(child.name), child %>
  <br/>

  <%= button_to_remote((!child.checkedin ? "signin" : "signout"),
    :update => "child_"+child.id.to_s, :script => true,
    :url=> {:controller=>'children',:action=>"checkin", :id => child.id}) %>


